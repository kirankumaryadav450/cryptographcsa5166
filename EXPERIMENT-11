#include <stdio.h>
#include <math.h>

int main()
{
    double keys = 1;

    for(int i=25;i>=1;i--)
        keys *= i;

    printf("Approximate Keys = %.2e\n", keys);

    double bits = log(keys)/log(2);
    printf("Equivalent to 2^%.2f\n", bits);

    return 0;
}
